export interface errorMsg {
    status: number;
    message: string;
}


export interface bookType {
    title: string;
    author: string;
    isbn: string;
    publishedYear:Date;
    favorite:boolean
}